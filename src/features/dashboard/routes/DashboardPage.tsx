import { FeaturePage } from "@/components/layout/FeaturePage";

export default function DashboardPage() {
  return (
    <FeaturePage
      title="Dashboard"
      description="Start here with an authenticated shell, shared providers, and a feature-first route structure."
    />
  );
}
